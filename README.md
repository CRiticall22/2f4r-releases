# 2F4R Releases

Build artifacts for the 2F4R ENGINE loader and DLL, published here via GitHub Releases so the loader can fetch the current build directly from a stable URL instead of requiring a local file path. See releases for downloads.
